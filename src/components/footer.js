import styles from '../css/footer.module.css';

export const Footer = () => {
    return (
        <footer className={styles.footer}>
            <small className={styles.copyright}>&copy; 2023 AY</small>
        </footer>
    );
};